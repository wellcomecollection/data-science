# Score

Uses the [Elo algorithm](https://en.m.wikipedia.org/wiki/Elo_rating_system) (used elsewhere for chess rankings, tinder matches, etc) to compare model performance, as judged subjectively in the [compare](../compare) module.
